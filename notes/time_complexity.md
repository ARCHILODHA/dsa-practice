# Time Complexity

O(1) → Constant  
O(log n) → Binary Search  
O(n) → Linear Scan  
O(n log n) → Sorting  
O(n^2) → Nested Loops  

## Tips
- Avoid nested loops when possible
- Use hashing to optimize
