# Prefix Sum Patterns

## Overview

Prefix Sum is a technique used to efficiently calculate the sum of elements over a range.

Instead of repeatedly calculating the sum of a subarray, we preprocess the array and answer range-sum queries in O(1).

## Basic Idea

Given:

```text
Array:
[2, 4, 1, 5, 3]
